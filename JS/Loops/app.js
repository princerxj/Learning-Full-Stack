//Loops : Used to iterate a piece of code 
/* For loops : 
    syntax : for(initialisation;condition;updation) {
                //Do something 
             }
*/

for(let i = 0; i <= 5; i++) {
    console.log(i);
}
